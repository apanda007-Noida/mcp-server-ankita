services:
  - type: web
    name: google-mcp-server
    env: python
    buildCommand: "pip install -r google-mcp-server/requirements.txt"
    startCommand: "cd google-mcp-server && uvicorn server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0

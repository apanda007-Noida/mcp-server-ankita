{
  "name": "root-project",
  "version": "1.0.0",
  "description": "Root package.json to trigger Nixpacks Node provider on Railway",
  "scripts": {
    "build": "npm --prefix google-mcp-server/dashboard install && npm --prefix google-mcp-server/dashboard run build"
  }
}

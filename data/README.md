# Local Data Storage

This folder is used to store temporary data, such as local JSON dumps of the scraped Google Play reviews, embedding cache, or other artifacts generated by the Reasoning Engine during development.

**Note:** Make sure that any `.json` or `.csv` files stored here are added to your `.gitignore` to prevent leaking raw user data into version control.
